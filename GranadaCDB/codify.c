
#include <stdlib.h>
#include <stdio.h>
#include "codify.h"

string* codify_data(string* data){
string* codified_data=copy_string(data);
return codified_data;
}
string* decodify_data(string* data){
string* decodified_data = copy_string(data);
return decodified_data;
}
