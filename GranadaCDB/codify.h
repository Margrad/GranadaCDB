/***
This part will be responsible for codification of the data when recorded on the hard drive
Empty for now and just return what they get
***/

#include "string.h"


string* codify_data(string* data);
string* decodify_data(string* data);
